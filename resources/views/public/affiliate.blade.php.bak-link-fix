@php
    $seoTitle = 'Program Affiliate';
    $seoDescription = 'Program affiliate publik Kampus Media untuk umum, komunitas, kreator, dan relasi kampus. Daftar tanpa akun mahasiswa.';
    $canonicalUrl = url('/affiliate');
    $affiliateRegisterUrl = request()->getHost() === 'affiliate.kampus.media' ? url('/daftar') : route('affiliate.register');
@endphp

<!doctype html>
<html lang="id" class="scroll-smooth">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ $seoTitle }} | Kampus Media</title>
    <meta name="description" content="{{ $seoDescription }}">
    <meta name="robots" content="index, follow">
    <link rel="canonical" href="{{ $canonicalUrl }}">
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="Kampus Media">
    <meta property="og:title" content="{{ $seoTitle }} | Kampus Media">
    <meta property="og:description" content="{{ $seoDescription }}">
    <meta property="og:url" content="{{ $canonicalUrl }}">
    <meta property="og:image" content="{{ url('/images/social/logo%20kampus%20media.png') }}">
    <meta name="twitter:card" content="summary_large_image">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        navy: '#071a3d',
                        gold: '#f59e0b',
                        tealx: '#0f766e',
                    },
                    boxShadow: {
                        soft: '0 24px 70px rgba(15, 23, 42, .12)',
                    },
                },
            },
        }
    </script>
</head>
<body class="bg-slate-50 text-slate-900 antialiased">
    <header class="sticky top-0 z-40 border-b border-slate-200 bg-white/90 px-4 py-3 backdrop-blur-xl sm:px-6 lg:px-8">
        <div class="mx-auto flex max-w-7xl items-center justify-between gap-4">
            <a href="{{ route('home') }}" class="flex items-center gap-3">
                <img src="/images/social/logo%20kampus%20media.png" alt="Kampus Media" class="h-11 w-auto">
                <span>
                    <strong class="block text-base font-black text-navy">Kampus Media</strong>
                    <small class="block text-xs font-bold text-slate-500">Affiliate Partner</small>
                </span>
            </a>
            <nav class="hidden items-center gap-6 text-sm font-bold text-slate-600 md:flex">
                <a href="#cara-kerja" class="hover:text-navy">Cara Kerja</a>
                <a href="#komisi" class="hover:text-navy">Komisi</a>
                <a href="{{ route('registration.create') }}" class="hover:text-navy">Form PMB</a>
            </nav>
            <a href="{{ $affiliateRegisterUrl }}" class="inline-flex items-center gap-2 rounded-full bg-tealx px-4 py-3 text-sm font-black text-white shadow-lg shadow-teal-700/20">
                <i data-lucide="user-plus" class="h-4 w-4"></i>
                Gabung
            </a>
        </div>
    </header>

    <main>
        <section class="relative overflow-hidden bg-navy text-white">
            <img src="https://images.unsplash.com/photo-1552664730-d307ca884978?auto=format&fit=crop&w=1800&q=80" alt="Komunitas affiliate Kampus Media" class="absolute inset-0 h-full w-full object-cover opacity-30">
            <div class="absolute inset-0 bg-gradient-to-r from-navy via-navy/92 to-navy/55"></div>
            <div class="relative mx-auto grid min-h-[calc(100vh-5rem)] max-w-7xl items-center gap-10 px-4 py-16 sm:px-6 lg:grid-cols-[1.05fr_.95fr] lg:px-8">
                <div>
                    <p class="inline-flex rounded-full border border-white/20 bg-white/10 px-4 py-2 text-sm font-black text-cyan-100">Khusus umum, non mahasiswa</p>
                    <h1 class="mt-5 max-w-4xl text-4xl font-black leading-tight sm:text-6xl">Jadi affiliate Kampus Media tanpa perlu login mahasiswa.</h1>
                    <p class="mt-5 max-w-2xl text-lg leading-8 text-sky-50">Bantu calon mahasiswa menemukan kampus, kelas, dan biaya kuliah yang cocok. Kamu cukup membagikan link referral dari tim Kampus Media, lalu setiap pendaftaran akan tercatat di sistem pusat.</p>
                    <div class="mt-8 flex flex-wrap gap-3">
                        <a href="{{ $affiliateRegisterUrl }}" class="inline-flex items-center gap-2 rounded-full bg-gold px-6 py-4 font-black text-navy shadow-xl shadow-orange-500/20 transition hover:-translate-y-1">
                            <i data-lucide="send" class="h-5 w-5"></i>
                            Daftar Jadi Affiliate
                        </a>
                        <a href="{{ route('registration.create') }}" class="inline-flex items-center gap-2 rounded-full border border-white/25 bg-white/10 px-6 py-4 font-black text-white backdrop-blur transition hover:-translate-y-1 hover:bg-white/15">
                            <i data-lucide="external-link" class="h-5 w-5"></i>
                            Lihat Form PMB
                        </a>
                    </div>
                </div>
                <aside class="rounded-3xl border border-white/15 bg-white/10 p-6 shadow-soft backdrop-blur-xl">
                    <p class="text-sm font-black uppercase tracking-wide text-cyan-100">Yang kamu dapat</p>
                    <div class="mt-5 grid gap-4">
                        @foreach ([['link', 'Link referral pribadi', 'Dibuatkan oleh admin Kampus Media dan bisa kamu bagikan ke publik.'], ['users-round', 'Tracking pendaftar', 'Lead dari link kamu masuk ke sistem pusat dengan kode affiliate.'], ['hand-coins', 'Potensi komisi', 'Komisi mengikuti aturan dan validasi pembayaran yang berlaku.']] as $item)
                            <div class="flex gap-4 rounded-2xl bg-white/10 p-4">
                                <span class="grid h-11 w-11 shrink-0 place-items-center rounded-2xl bg-white text-navy">
                                    <i data-lucide="{{ $item[0] }}" class="h-5 w-5"></i>
                                </span>
                                <div>
                                    <strong class="block font-black">{{ $item[1] }}</strong>
                                    <p class="mt-1 text-sm leading-6 text-sky-50">{{ $item[2] }}</p>
                                </div>
                            </div>
                        @endforeach
                    </div>
                </aside>
            </div>
        </section>

        <section id="cara-kerja" class="px-4 py-16 sm:px-6 lg:px-8 lg:py-24">
            <div class="mx-auto max-w-7xl">
                <div class="max-w-3xl">
                    <p class="text-sm font-black uppercase tracking-wide text-tealx">Cara Kerja</p>
                    <h2 class="mt-3 text-3xl font-black text-navy sm:text-5xl">Affiliate publik dibuat untuk relasi, komunitas, dan kreator.</h2>
                </div>
                <div class="mt-10 grid gap-5 md:grid-cols-4">
                    @foreach ([['1', 'Hubungi admin', 'Ajukan nama dan nomor WhatsApp untuk dibuatkan kode affiliate.'], ['2', 'Terima link referral', 'Admin membuat partner dan link daftar khusus untuk kamu.'], ['3', 'Bagikan ke publik', 'Sebarkan ke komunitas, sosial media, teman kerja, atau keluarga.'], ['4', 'Pantau hasil', 'Tim memvalidasi pendaftar dan pembayaran dari sistem pusat.']] as $step)
                        <article class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
                            <span class="grid h-12 w-12 place-items-center rounded-2xl bg-navy text-lg font-black text-white">{{ $step[0] }}</span>
                            <h3 class="mt-5 text-xl font-black text-navy">{{ $step[1] }}</h3>
                            <p class="mt-3 leading-7 text-slate-600">{{ $step[2] }}</p>
                        </article>
                    @endforeach
                </div>
            </div>
        </section>

        <section id="komisi" class="bg-white px-4 py-16 sm:px-6 lg:px-8 lg:py-24">
            <div class="mx-auto grid max-w-7xl gap-8 lg:grid-cols-[.85fr_1.15fr] lg:items-center">
                <div>
                    <p class="text-sm font-black uppercase tracking-wide text-tealx">Komisi</p>
                    <h2 class="mt-3 text-3xl font-black text-navy sm:text-5xl">Tercatat rapi dari kode referral.</h2>
                    <p class="mt-5 leading-8 text-slate-600">Setiap calon mahasiswa yang daftar melalui link referral akan membawa kode affiliate. Setelah pembayaran tervalidasi, status komisi dapat dikelola oleh tim admin melalui dashboard Kampus Media.</p>
                    <a href="{{ $affiliateRegisterUrl }}" class="mt-7 inline-flex rounded-full bg-tealx px-6 py-4 font-black text-white shadow-lg shadow-teal-700/20">Daftar dan Verifikasi Email</a>
                </div>
                <div class="grid gap-4 sm:grid-cols-2">
                    @foreach ([['Target publik', 'Tidak perlu akun mahasiswa untuk mulai mendaftar sebagai affiliate.'], ['Kode unik', 'Setiap partner memiliki kode referral yang berbeda.'], ['Form PMB resmi', 'Pendaftar tetap masuk lewat form resmi Kampus Media.'], ['Validasi admin', 'Komisi mengikuti hasil verifikasi pembayaran dan kebijakan internal.']] as $card)
                        <article class="rounded-3xl border border-slate-200 bg-slate-50 p-6">
                            <strong class="block text-xl font-black text-navy">{{ $card[0] }}</strong>
                            <p class="mt-3 leading-7 text-slate-600">{{ $card[1] }}</p>
                        </article>
                    @endforeach
                </div>
            </div>
        </section>
    </main>

    <script>
        lucide.createIcons();
    </script>
</body>
</html>
